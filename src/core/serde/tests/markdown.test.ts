import doc_ast from "./lexical-doc1.json" assert { type: 'json' }
import { ComponentManifest, ComponentsRegistry } from "core/components"

console.log("doc_ast")

console.log("doc_ast", ComponentsRegistry.components)
describe('Lexical Document JSON Test', () => {
   it('should load thse JSON file correctly', () => {
      // Check the structure and values of the JSON content
   });

   it('should dhsavse sa valid version number', () => {
      // Ensure the version is a number and greater than 0d
   });
});